## Flow Input v4.2.15 更新说明

### AI 服务与账号体验

1. 所有套餐均可配置自定义 OpenAI 兼容服务，填写完整 URL 和 API Key 后直接使用自己的服务，不消耗 FlowGuide AI 的订阅额度或余额。
2. 优化 FlowGuide AI 网关的订阅分组选择，支持订阅额度耗尽后按明确错误码切换到余额分组，并完善缓存刷新与账号套餐状态展示。
3. 增加中英文自定义 AI 服务、套餐升级和计费状态文案，补充 FlowGuideAI 中央集成及 BYOK 计费边界说明。

### 常用语与标签

1. 支持从标签管理直接打开对应的常用语列表，新建常用语时自动继承当前标签。
2. 内置标签会根据当前语言显示本地化名称，同时保留用户自定义标签原名。

### 窗口体验

1. 配置窗口不再始终置顶，避免遮挡其他应用。

### 验证

1. 完成 89 个 Node 测试及生产构建验证。

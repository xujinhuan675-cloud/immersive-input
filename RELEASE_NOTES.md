## Flow Input v4.2.14 更新内容

### 账号与登录

- 修复登录凭证过期后可能无法自动刷新的问题，减少需要手动重新登录的情况。
- 当服务端返回凭证过期提示时，应用会尝试刷新访问令牌并自动重试请求。

### AI 服务稳定性

- 改进 FlowGuide / Sub2API 请求中的认证恢复流程，过期令牌刷新后会继续使用新的访问凭证。
- 账号页面在刷新凭证后会同步最新登录状态，让会员、额度和服务请求状态更一致。

### 测试覆盖

- 新增过期认证令牌刷新相关测试，覆盖本地令牌读取、服务端 401 / 过期响应后的刷新与重试路径。
